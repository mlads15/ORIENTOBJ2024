<?php

/*class Veiculo {
    private $capacidade;
    private $venderPassagem;

    public function __construct($capacidade) {
        $this->capacidade = $capacidade;
        $this->venderPassagem = 0;
    }

    public function venderPassagem($capacidade){
        if($this->capacidade >= ($this->passagensVendidas + $quantidade)) {
            $this->passagensVendidas;
        }
    }

    public function getVenderPassagem() {
        return $this->venderPassagem();
    }


}

$capacidade = readline("Informe a capacidade do veículo: ");
$onibus = new Veiculo($capacidade);

while(true) {

    echo "\n";
    $quantidPassagem = readline("Informe a quantidade de passageiros: ");
    

    if(quantidPassagem <= 0)
    break;

    $passagemVendidas = $onibus->venderPassagem($quantidPassagem);
    if($passagemVendidas) {
        echo "Passagens vendidas com sucesso!";
    } else 
        echo "Não foi possível realizar a venda dessa passagem.";
}

*/